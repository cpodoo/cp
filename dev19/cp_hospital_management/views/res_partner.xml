<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_vendor_inherited_form_view" model="ir.ui.view">
        <field name="name">Res partner</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
			<xpath expr = "//form/sheet/group" position = 'before'>
				<div class="o_row">
					<label for="vendor_type" string="Vendor Type" invisible="context.get('customer_key') or is_patient or is_doctor"/>
					<div class = "col-12">
						<field name="vendor_type" widget="selection" force_save="1" invisible="context.get('customer_key') or is_patient or is_doctor" readonly="id"/>
						<field name="boolean_check" invisible="1"/>
					</div>
				</div>
			</xpath>
			<xpath expr = "//field[@name = 'note']" position = 'after'>
                <field name="allowed_surgeon"/>
			</xpath>
			<xpath expr = "//page[@name = 'practising_details']" position = 'after'>
                <page name = 'allowed_surgeon' string="Surgeons" invisible="not allowed_surgeon">
                    <field name="allowed_surgeon_ids">
                        <list editable="bottom">
                            <field name="surgeon_id"/>
                        </list>
                    </field>
                </page>
			</xpath>
        </field>
    </record>

    <record id="account.res_partner_action_customer" model="ir.actions.act_window">
        <field name="name">Customers</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_customer': 1,'res_partner_search_mode': 'customer', 'default_is_company': True, 'default_customer_rank': 1,'customer_key': 'customer'}</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create a new customer in your address book
          </p><p>
            Odoo helps you easily track all activities related to a customer.
          </p>
        </field>
    </record>

    <record id="account.res_partner_action_supplier" model="ir.actions.act_window">
        <field name="name">Vendors</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_supplier': 1,'res_partner_search_mode': 'supplier', 'default_is_company': True, 'default_supplier_rank': 1,'vendor_key': 'vendor'}</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create a new supplier in your address book
          </p><p>
            Odoo helps you easily track all activities related to a supplier.
          </p>
        </field>
    </record>

</odoo>